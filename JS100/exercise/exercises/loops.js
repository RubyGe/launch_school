let fish = ['Dory', 'Marlin', 'Gill', 'Nemo', 'Bruce'];

for (let index = 0; index < fish.length; index += 1) {
  if (fish[index] === 'Nemo') {
    console.log(fish[index]);
    break;
  } else {
    console.log(fish[index]);
  }
}