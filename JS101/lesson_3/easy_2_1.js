let arr = [1, 2, 3];
let newArr = arr;

const num = arr[0];
let newNum = num;

function double(num) {
  return num * 2;
}

double(newNum);

 variables, primitive values, and objects

 arr, newArr, num, newNum, double, num

 primitive values:

 1, 2, 3, num, newNum, num, 2 (0 in arr[0], double(newNum))

 objects

 arr, newArr, double


 There are 6 variables, 9 primitive values, and 2 objects. I